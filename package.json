{
  "name": "neuropay-ai",
  "version": "1.0.0",
  "private": true,
  "description": "NeuroPay AI — multi-tenant field-service management platform with AI-assisted workflows",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace=@neuropay/api && npm run build --workspace=@neuropay/web",
    "test": "npm run test --workspace=@neuropay/api",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "start": "npm run start --workspace=@neuropay/api"
  },
  "engines": { "node": ">=18" }
}
